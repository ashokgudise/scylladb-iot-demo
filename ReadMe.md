

````
    -- Create SQL Keyspae, Table commands using csqlsh.sql
    
    
    # Build Code
        mvn clean package -DskipTests=true

    #Validate the data in scyllaDB
    
    select * from sensor_readings
````